# سجل العضو 2 — صفحات الطالب (الواجهة العامة)

> ملف واحد فقط طوال المشروع. أضف إدخال جديد أعلى الملف (الأحدث بالأعلى) بعد كل جلسة عمل.
> لا تحذف الإدخالات القديمة.
> ⚠️ الملفات المملوكة لك محددة بخطة البناء (team-plan-v2.md) القسم 3. لا تعدّل أي ملف خارجها.

---

## [التاريخ: 2026-07-19 — إعادة تسليم بعد الجولة 1]

### ما تم إنجازه
- أعدت تطبيق نفس تسليمي السابق (`SubjectList.jsx`, `Subject.jsx`, `StudyPlan.jsx`, ومكوّنات `components/subject/*`) فوق حزمة `round1-approved`، لأن الحزمة اللي استلمتها رجّعت ملفاتي الثلاثة (الصفحات) لنسخة الـ Placeholder الأصلية من اليوم 0 — بينما `components/subject/*` وصلت سليمة بدون تغيير.
- التفاصيل الوظيفية نفسها من الإدخال السابق (قائمة + بحث Fuse.js + فلترة hidden + عارض iframe لكل محاضرة).

### مشكلات واجهتها
- ⚠️ حزمة `round1-approved` لا تحتوي تسليمي الفعلي لملفات الصفحات الثلاثة ولا لإدخال سجلّي السابق (رجعا لحالة placeholder/فارغة)، رغم إنها تحتوي تحديثات فعلية لأعضاء آخرين (1، 3، 5) وبيانات بذرة جديدة من عضو 4 (`lectures-prof-ali.json`). أبلغ المدير بهذا صراحة بردّي.

### قرارات اتخذتها (ولماذا)
- لم أغيّر أي منطق؛ فقط أعدت نفس الملفات المُسلَّمة سابقاً لتفادي فقد العمل مرة ثانية.

### هل احتجت تعديل ملف خارج نطاق ملكيتك؟ (يُفترض دائماً "لا" بهذا التصميم)
- لا.

### ملاحظات للمدير
- الرجاء التأكد إن كان سقوط تسليمي بحزمة `round1-approved` مقصوداً (تراجع عن قرار) أو خطأ بعملية الدمج — وتأكيد افتراضَي مصدر قائمة المواد ومسار PDF المذكورين بإدخالي السابق.

---
